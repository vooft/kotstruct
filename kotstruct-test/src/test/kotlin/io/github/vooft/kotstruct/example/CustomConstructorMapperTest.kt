package io.github.vooft.kotstruct.example

/*
import io.kotest.matchers.shouldBe
import org.junit.jupiter.api.Test
import java.util.UUID

class CustomConstructorMapperTest {
    @Test
    fun `should map with custom constructor`() {
        val mapper = KotStructGeneratedCustomConstructorMapper()

        val from = CustomConstructorMapper.FromDto(id = UUID.randomUUID(), extraProperty = UUID.randomUUID())

        val to = mapper.map(from)

        to.id shouldBe from.id
        to.name shouldBe CUSTOM_CONSTRUCTOR_DEFAULT_NAME
    }
}
*/
